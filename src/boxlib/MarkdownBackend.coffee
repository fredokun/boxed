##
#
##

define([],(() ->
        
        #
        class MarkdownBackend extends Backend

                # @method constructor : 
                constructor : (callback) ->
                        super(callback)
                        
                # @method chomp :
                # arg box :
                chomp : (box) ->
                        

))
