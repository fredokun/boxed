##
#
#(Abstract) class that represent a Backend.
#
##

define([],(() ->

        class Backend

                # 
                constructor : (@callback) ->
                

                # 
                # arg box :
                chomp : (box) ->
                      throw error
))
